#include <bits/stdc++.h>
using namespace std;
#include <atcoder/all>
using namespace atcoder;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
using P = pair<int,int>;

int main() {
  // int N;
  // string S;
  // cin >> N;
  // int t = 0, a = 0;
  // for (int i = 0; i < N; i++) {
  //   if(S[i] == 'T') ++t;
  //   else ++a;
  // }

  // if (t > a) {
  //   cout << 'T' << endl;
  // } else if (t < a) {
  //   cout << 'A' << endl;
  // }
  return 0;
}
